#!/bin/bash

DIR="/Users/tbenzige/Projects/gitdrop"
BASE=`basename $1`

cp $1 $DIR
cd $DIR
git add $BASE
git commit -m "$BASE added"
git push origin gh-pages
echo "http://tylerbenziger.github.com/gitdrop/$BASE" | pbcopy
